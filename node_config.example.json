{
  // ===========================
  // Agent Node 配置模板（JSONC）
  // 说明：
  // - 本文件支持 // 与 /* */ 注释（agent node 会自动剥离注释后再解析 JSON）。
  // - 兼容：旧文件名 proxy_config.json 仍可用；旧字段 proxy_id/proxy_token 仍可用。
  // ===========================
  //
  "manager_ws": "ws://MANAGER_IP:8765",
  // 新推荐字段（对外概念）
  "node_id": "home_hw",
  "node_token": "",
  // 兼容字段（保留即可，不必同时设置）
  // "proxy_id": "home_hw",
  // "proxy_token": "",
  "max_pending": 10,
  // sandbox：控制文件系统/命令能力
  // 常用：workspaceWrite（允许在工作区写）
  "sandbox": "workspaceWrite",
  // 高危：dangerFullAccess（几乎全开）
  // "sandbox": "dangerFullAccess",
  //
  // approval_policy：控制是否触发“审批”
  // onRequest：需要审批（会把审批转发到 Telegram）
  // "approval_policy": "onRequest",
  //
  // unlessTrusted：默认（仅在不受信时要求审批）
  "approval_policy": "unlessTrusted",
  // never：不走审批（高危）
  // "approval_policy": "never",
  "codex_cwd": "/root/telegram-bot",
  "codex_bin": "codex",
  "http_proxy": "http://127.0.0.1:8080",
  "https_proxy": "http://127.0.0.1:8080",
  "no_proxy": "localhost,127.0.0.1,::1"
}

